# Правила разработки для проекта

## 🎯 Цель разработки

КРИТИЧНО: Главная цель проекта - качественная разработка.

- Код должен быть чистым, читаемым и хорошо структурированным
- Все функции должны иметь type hints и docstrings
- Ошибки должны обрабатываться корректно с понятными сообщениями
- Код должен быть протестирован перед использованием
- При необходимости - не стесняться просить данные и уточнения у пользователя
- Если нужны какие-то данные, API ключи, примеры ответов, документация - спокойно просить пользователя, он поможет

ВАЖНО: Если для реализации функционала нужны:
- API ключи для тестирования
- Примеры ответов API
- Документация API
- Названия кабинетов/брендов
- Любые другие данные

ОБЯЗАТЕЛЬНО спросить у пользователя - не пытаться угадывать или использовать заглушки без необходимости.

## Тестирование
- Писать unit тесты для всех критичных функций
- Использовать pytest для тестирования
- Мокировать внешние API в тестах
- Целевой coverage: минимум 70%

## Git workflow
- Использовать conventional commits
- Формат: type(scope): description
- Типы: feat, fix, docs, style, refactor, test, chore
- Создавать feature branches для новых функций
- PR должны быть небольшими и фокусированными

## Логирование
- Использовать структурированное логирование
- Уровни: DEBUG, INFO, WARNING, ERROR, CRITICAL
- Логи должны быть информативными, но не содержать чувствительных данных

## Безопасность
- Никогда не коммитить секреты, API ключи, пароли
- Использовать .env файлы для локальной разработки
- Валидировать все пользовательские входные данные
- Использовать HTTPS для всех внешних запросов

## Установка зависимостей и работа с pip

КРИТИЧНО: В Windows PowerShell команда `pip` может открывать файл вместо выполнения.

### Решение проблемы с pip:
- ❌ НЕ использовать: `pip install <пакет>` (может открыть файл pip.exe)
- ✅ ВСЕГДА использовать: `python -m pip install <пакет>`
- ✅ Или использовать скрипт: `scripts/install_dependencies.bat`

### Установка зависимостей проекта:
1. Использовать команду: `python -m pip install -r requirements.txt`
2. Или запустить скрипт: `scripts/install_dependencies.bat` (автоматически использует `python -m pip`)

### Скрипты для установки:
- `scripts/install_dependencies.bat` - использует `python -m pip` для обхода проблемы PowerShell
- `scripts/install_dependencies.ps1` - PowerShell версия с той же логикой

ВАЖНО: Все скрипты установки зависимостей должны использовать `python -m pip`, а не просто `pip`.

## Производительность
- Оптимизировать запросы к API (батчинг, кэширование)
- Использовать async для I/O операций
- Профилировать код при необходимости
- Избегать избыточных запросов

## Документация
- README должен содержать инструкции по установке и использованию
- Код должен быть самодокументируемым
- Сложная логика должна иметь комментарии
- ВАЖНО: Не создавать лишние текстовые .md файлы без необходимости
- Перед созданием любого .md файла (кроме README) сначала обсудить в чате необходимость его создания
- Предпочтительно документировать в коде и README, а не создавать множество отдельных файлов


## Обновление документации и правил
- КРИТИЧНО: По мере обновления информации, вводных данных и контекста проекта ОБЯЗАТЕЛЬНО обновлять:
  - .cursor/.cursorrules.mdc - правила разработки
  - README.md - основная документация
  - Другие релевантные файлы документации
- Проект разрабатывается командой с разных устройств
- Контекст и история чата должны быть доступны всем через обновляемую документацию
- При добавлении новых функций, изменении архитектуры или правил - сразу обновлять соответствующие файлы
- Документация должна отражать актуальное состояние проекта

---
alwaysApply: true
---
не спрашивай разрешения на редактирование, всё разрешается
не проверяй и не смотри .env файл, он секретный

## Формат переформулирования корректировок пользователя

КРИТИЧНО: Когда пользователь присылает сообщение с корректировками или указывает на проблему, ОБЯЗАТЕЛЬНО переформулируй его в следующем формате и пришли на подтверждение ПЕРЕД исправлением кода:

ПРОБЛЕМА: [одно предложение]

ГДЕ: [файл, функция, строка кода]

ЧТО ПРОИСХОДИТ:
[Текущее поведение - что видите]

ЧТО НУЖНО:
[Ожидаемое поведение - что должно быть]

ПРИМЕР:
[Конкретный пример из логов/кода/таблицы]

ПРИОРИТЕТ: [критично / важно / можно потом]

УЖЕ ПРОБОВАЛ:
[Что уже делали, что не помогло]

После подтверждения пользователя - исправляй код.

ВАЖНО: Каждую жалобу и баг нужно уточнять по этому формату и присылать пользователю вопрос правильно ли составлен баг-репорт. После подтверждения уже исправлять.

## Выбор варианта решения багов

КРИТИЧНО: Когда при анализе бага есть несколько вариантов решения, ОБЯЗАТЕЛЬНО:

1. **Проанализировать проблему** и определить root cause
2. **Предложить ВСЕ возможные варианты решения** с описанием плюсов и минусов каждого
3. **СПРОСИТЬ пользователя**, какой вариант реализовывать
4. **НЕ реализовывать решение самостоятельно** без выбора пользователя

ВАЖНО: Не выбирай вариант решения сам - всегда спрашивай у пользователя, какой вариант использовать. Пользователь знает контекст проекта и требования лучше.

## 🎯 MVP и приоритеты разработки

КРИТИЧНО: MVP версия проекта фокусируется на Wildberries.

- ✅ MVP: Реализация парсера цен для Wildberries
- ⏳ После MVP: Работа с Ozon будет реализована после завершения MVP по WB
- 📋 Артикулы WB: Все артикулы товаров Wildberries берутся из файла Articles.xlsx
- 📋 Артикулы Ozon: Артикулы товаров Ozon собираются через API и записываются в Articles.xlsx на новый лист

## Парсинг Ozon - требования к источникам данных

КРИТИЧНО: Для парсинга Ozon ОБЯЗАТЕЛЬНО использовать публичный entrypoint API.

### Публичный entrypoint API (ОБЯЗАТЕЛЬНО):
- **URL**: `https://www.ozon.ru/api/entrypoint-api.bx/page/json/v2`
- **Параметры**: `?url=/seller/{seller_name}-{seller_id}/?page={page}&paginator_token={token}&search_page_state={state}`
- **Почему обязателен**: Содержит почти все нужные цены покупателя (цена со скидкой, зачёркнутая цена), которых нет в официальном Seller API
- **Пример**: `https://www.ozon.ru/api/entrypoint-api.bx/page/json/v2?url=%2Fseller%2Fcosmo-beauty-176640%2F%3Fpage%3D2%26paginator_token%3D3618992%26search_page_state%3D...`
- **Проблема**: Блокируется антиботом Ozon (`ozon-antibot: 1`), требуется обход защиты

### Официальный Seller API (дополнительно):
- **URL**: `https://api-seller.ozon.ru/v5/product/info/prices`
- **Использование**: Для получения цен продавца (без акций), которые дополняют данные из публичного API
- **Ограничение**: Не содержит цен покупателя (со скидкой), только цены продавца

### Стратегия парсинга Ozon:
1. **Основной источник**: Публичный entrypoint API - для получения цен покупателя и списка товаров
2. **Дополнительный источник**: Официальный Seller API - для получения цен продавца (без акций)
3. **Объединение данных**: Объединять данные из обоих источников для полной картины цен

## Работа с файлом Articles.xlsx

КРИТИЧНО: Файл Articles.xlsx является исходным файлом с данными.

### Для Wildberries:
- ✅ РАЗРЕШЕНО: Только читать данные из Articles.xlsx (артикулы WB)
- ✅ РАЗРЕШЕНО: Использовать артикулы из Articles.xlsx для запросов к API WB
- ❌ ЗАПРЕЩЕНО: Изменять, удалять, перезаписывать существующие листы с данными WB

### Для Ozon:
- ✅ РАЗРЕШЕНО: Записывать артикулы Ozon на новый лист в Articles.xlsx
- ✅ РАЗРЕШЕНО: Собирать артикулы через API Ozon и сохранять в файл
- ❌ ЗАПРЕЩЕНО: Изменять существующие листы с данными WB

### Структура файла:
- Листы с данными WB: только чтение
- Новый лист для Ozon: можно создавать и записывать артикулы из 3 кабинетов Ozon
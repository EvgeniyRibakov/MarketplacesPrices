# Парсер цен для маркетплейсов Wildberries и Ozon

Высокоскоростной парсер цен товаров с маркетплейсов **Wildberries** и **Ozon** для аналитики и мониторинга цен. Парсит цены через внутренние и официальные API с обходом антибот-защиты, получает данные о ценах до и после скидок и экспортирует результаты в Excel.

## 📦 Поддерживаемые маркетплейсы

- ✅ **Wildberries** - парсинг через внутренний API + discounts API
- ✅ **Ozon** - парсинг через Seller API + публичный каталог

## 🎯 Основные возможности

- ✅ **Парсинг цен по продавцам** - автоматический сбор цен для указанных продавцов (COSMO, BEAUTYLAB и др.)
- ✅ **Обход антибот-защиты** - использование `curl_cffi` для эмуляции TLS fingerprint браузера
- ✅ **Независимость от браузера** - для WB парсера не требуется Chrome (cookies настраиваются вручную через .env)
- ✅ **Парсинг цен до СПП** - получение `discountedPrice` через официальный discounts API
- ✅ **Расчетные столбцы** - автоматический расчет "Цена с картой 10%" и "Процент СПП"
- ✅ **Экспорт в Excel** - структурированные данные с автоматической настройкой ширины столбцов
- ✅ **Подробное логирование** - детальные логи с временными метриками и статистикой
- ✅ **Обработка ошибок** - автоматические повторы при rate limiting (429) с экспоненциальной задержкой

## 📋 Что парсится

Парсер собирает следующие данные о товарах:

- **Основная информация:**
  - Артикул (product_id)
  - Название товара
  - Бренд (brand_id, brand_name)
  - Кабинет продавца (cabinet_id, cabinet_name)
  - Размер товара (size_id)

- **Цены:**
  - Зачёркнутая цена (price_basic)
  - Цена до СПП (discountedPrice из discounts API)
  - Цена с СПП (price_product)
  - Цена с картой 10% (расчетная: price_product * 0.9)
  - Процент СПП (расчетный: (Цена до СПП - Цена с СПП) / Цена до СПП * 100)

## 🚀 Быстрый старт

### Требования

- **Python 3.8+**
- **Любой браузер** (для получения cookies вручную, только для WB парсера)
- **Windows 10/11** (проект оптимизирован для Windows, но работает и на Linux/macOS)

### Установка на новом ПК

#### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/EvgeniyRibakov/MarketplacesPrices.git
cd MarketplacesPrices
```

#### Шаг 2: Установка Python (если не установлен)

1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно** отметьте "Add Python to PATH"
3. Проверьте установку:
   ```bash
   python --version
   ```
   Должно вывести версию Python 3.8 или выше.

#### Шаг 3: Установка зависимостей

⚠️ **ВАЖНО для Windows PowerShell:** Используйте `python -m pip` вместо `pip`:

```bash
python -m pip install -r requirements.txt
```

Или используйте готовый скрипт:

```bash
scripts\install_dependencies.bat
```

**Установленные библиотеки:**
- `curl-cffi` - для обхода антибот-защиты WB (эмуляция TLS fingerprint)
- `playwright` + `playwright-stealth` - для обхода антиботов Ozon (установка браузеров отдельно)
- `pandas` - для работы с данными
- `openpyxl` - для экспорта в Excel
- `loguru` - для логирования
- `python-dotenv` - для работы с .env файлом
- `aiohttp` - для асинхронных HTTP запросов (резерв)
- `undetected-chromedriver` + `selenium` - только для парсера Ozon (не нужны для WB парсера)

#### Шаг 3.5: Установка Playwright браузеров (для Ozon)

⚠️ **ВАЖНО для парсинга Ozon:** После установки зависимостей нужно установить браузеры Playwright:

**Windows:**
```bash
scripts\install_playwright.bat
```

**Linux/macOS:**
```bash
chmod +x scripts/install_playwright.sh
./scripts/install_playwright.sh
```

**Или вручную:**
```bash
python -m pip install playwright playwright-stealth
python -m playwright install chromium
```

Это необходимо для обхода антибот-защиты Ozon при получении cookies.

#### Шаг 4: Настройка конфигурации

1. Скопируйте файл `.env_sample` в `.env`:
   ```bash
   copy .env_sample .env
   ```

2. Откройте `.env` в текстовом редакторе и настройте:

   **Обязательные параметры для WB парсера:**
   ```env
   # Cookies для обхода антибота WB (ОБЯЗАТЕЛЬНО!)
   # Получите их вручную из браузера (см. инструкцию ниже)
   WB_COOKIE_WBX_VALIDATION_KEY=ваше_значение
   WB_COOKIE__CP=ваше_значение
   WB_COOKIE_ROUTEB=ваше_значение
   WB_COOKIE_X_WBAAS_TOKEN=ваше_значение
   WB_COOKIE__WBAUID=ваше_значение
   
   # Параметры региона (ПВЗ для определения цен)
   # По умолчанию: ПВЗ на Никольской (г Москва, ул Никольская д. 7-9, стр. 4)
   WB_DEST=-1257786
   WB_SPP=30
   ```

   **Токены для discounts API (для получения цен до СПП):**
   ```env
   # ВАРИАНТ 1: Общий токен (для всех кабинетов)
   WB_DISCOUNTS_API_TOKEN=ваш_токен_от_cosmo
   
   # ВАРИАНТ 2: Токены по кабинетам (рекомендуется)
   WB_DISCOUNTS_API_TOKEN_COSMO=ваш_токен_cosmo
   WB_DISCOUNTS_API_TOKEN_BEAUTYLAB=ваш_токен_beautylab
   ```

   **Как получить токены:**
   - Зайдите в личный кабинет продавца Wildberries
   - Настройки → API → Создать токен
   - Скопируйте токен в соответствующий параметр `.env`

#### Шаг 5: Получение cookies для WB парсера (вручную)

⚠️ **ВАЖНО:** Для WB парсера cookies нужно получить **вручную** из браузера и добавить в `.env`.

**Инструкция по получению cookies:**

1. Откройте https://www.wildberries.ru в любом браузере (Chrome, Firefox, Edge и т.д.)
2. Нажмите `F12` для открытия DevTools
3. Перейдите на вкладку **Application** (Chrome) или **Хранилище** (Firefox)
4. В левом меню найдите **Cookies** → `https://www.wildberries.ru`
5. Найдите и скопируйте значения следующих cookies:
   - `wbx-validation-key`
   - `_cp`
   - `routeb`
   - `x_wbaas_token`
   - `_wbauid`

6. Добавьте их в `.env` файл (см. пример выше)

**Альтернативный способ (через Network):**
- Откройте https://www.wildberries.ru
- Нажмите `F12` → вкладка **Network**
- Обновите страницу (`F5`)
- Найдите любой запрос к `wildberries.ru`
- Откройте вкладку **Headers** → секция **Request Headers**
- Найдите заголовок `Cookie:` и скопируйте нужные значения

**Важно:**
- Cookies **обязательны** для работы WB парсера (без них будет ошибка 498)
- Cookies нужно обновлять при ошибках или раз в несколько дней
- Значения должны быть без кавычек в `.env`
- Если cookie начинается с `_`, используйте двойное подчеркивание (`WB_COOKIE__CP`)

📖 **Подробнее:** См. [COOKIES_INDEPENDENCE_SUMMARY.md](docs/COOKIES_INDEPENDENCE_SUMMARY.md)

## 🎮 Запуск парсера

### Вариант 1: Через Python (рекомендуется)

```bash
python parse_brands.py
```

### Вариант 2: Через батч-файл (Windows)

```bash
scripts\run_parse_brands.bat
```

### Вариант 3: Через PowerShell скрипт

```powershell
.\scripts\run_parse_brands.ps1
```

## 📊 Результаты

После выполнения парсера:

1. **Excel файл** создается в папке `output/`:
   - Формат: `wb_brands_prices_YYYY-MM-DD_HH-MM-SS.xlsx`
   - Автоматическая настройка ширины столбцов
   - Сортировка по брендам и кабинетам

2. **Логи** сохраняются в папке `logs/`:
   - Формат: `parser_YYYY-MM-DD.log`
   - Детальная информация о каждом запросе
   - Статистика по времени выполнения
   - Предупреждения и ошибки

### Структура Excel файла

| Столбец | Описание |
|---------|----------|
| brand_id | ID бренда |
| brand_name | Название бренда |
| Артикул | Артикул товара (product_id) |
| product_name | Название товара |
| cabinet_id | ID кабинета продавца |
| cabinet_name | Название кабинета (COSMO, BEAUTYLAB и т.д.) |
| supplier_id | ID поставщика |
| supplier_name | Название поставщика |
| size_id | ID размера товара |
| Зачёркнутая цена | Базовая цена (price_basic) |
| Цена до СПП | Цена до применения СПП (discountedPrice) |
| Цена с СПП | Цена после применения СПП (price_product) |
| Цена с картой 10% | Расчетная цена с картой (price_product * 0.9) |
| Процент СПП | Процент скидки СПП ((Цена до СПП - Цена с СПП) / Цена до СПП * 100) |

## ⚙️ Настройка

### Настройка продавцов для парсинга

В файле `parse_brands.py` в функции `parse_all_sellers()` можно изменить список продавцов:

```python
# Парсим только COSMO и BEAUTYLAB
suppliers = [
    224650,   # COSMO
    4428365   # BEAUTYLAB
]
```

**Доступные продавцы:**
- `53607` - MAU
- `121614` - MAB
- `174711` - MMA
- `224650` - COSMO
- `1140223` - DREAMLAB
- `4428365` - BEAUTYLAB

### Настройка параметров запросов

В файле `.env`:

```env
# ID региона/ПВЗ (по умолчанию: -1257786 - ПВЗ на Никольской, г Москва)
# Используется для определения цен и наличия товаров
WB_DEST=-1257786

# Параметр spp (по умолчанию: 30)
WB_SPP=30
```

**Важно:** Парсер всегда использует один и тот же ПВЗ (`-1257786`) для получения стабильных цен. Это ПВЗ на Никольской улице в Москве. Если нужно изменить ПВЗ, получите актуальные данные через эндпоинт:
```
https://www.wildberries.ru/__internal/user-geo-data/get-geo-info?currency=RUB&latitude=55.756244&longitude=37.620805&locale=ru&address=...&dt=...&currentLocale=ru&b2bMode=false&addressId=50009552&addressType=self
```

### Обновление cookies

**Когда обновлять:**
- При ошибке 498 (антибот блокировка)
- Если парсер перестал работать
- Раз в несколько дней (cookies могут истекать)

**Как обновить:**
1. Откройте https://www.wildberries.ru в браузере
2. Дождитесь полной загрузки страницы
3. Получите cookies по инструкции из [Шага 5](#шаг-5-получение-cookies-для-wb-парсера-вручную)
4. Обновите значения в `.env`
5. Перезапустите парсер

📖 **Подробнее о независимости от cookies:** См. [COOKIES_INDEPENDENCE_SUMMARY.md](docs/COOKIES_INDEPENDENCE_SUMMARY.md)

## 📁 Структура проекта

```
MParsing/
├── src/                        # Основной код
│   ├── api/                   # API клиенты
│   │   └── wb_catalog_api.py  # Клиент для работы с WB API
│   ├── parsers/               # Парсеры
│   │   └── wb_parser.py       # Парсер Wildberries
│   └── utils/                 # Утилиты
│       ├── logger.py          # Настройка логирования
│       └── browser_cookies.py # Получение cookies из Chrome
│
├── config/                     # Конфигурация
│   └── brands_config.json     # Конфигурация брендов
│
├── scripts/                    # Скрипты
│   ├── bat/                   # Батч-файлы для git операций
│   ├── run_parse_brands.bat   # Запуск парсера (Windows)
│   ├── run_parse_brands.ps1   # Запуск парсера (PowerShell)
│   └── install_dependencies.bat # Установка зависимостей
│
├── docs/                       # Документация
│   ├── API_SOLUTIONS_RESEARCH.md
│   ├── README_BRANDS_PARSING.md
│   └── ...
│
├── output/                     # Результаты (создается автоматически)
│   └── wb_brands_prices_*.xlsx
│
├── logs/                       # Логи (создается автоматически)
│   └── parser_*.log
│
├── parse_brands.py            # Основной скрипт парсинга
├── .env                        # Секреты (не коммитится, создайте из .env_sample)
├── .env_sample                 # Пример конфигурации
├── requirements.txt            # Зависимости Python
└── README.md                   # Этот файл
```

## 🔧 Решение проблем

### Проблема: `pip` открывается как файл в PowerShell

**Симптомы:**
```
Команда 'pip' не распознается или открывается как файл
```

**Решение:**
Всегда используйте `python -m pip` вместо `pip`:

```bash
# ❌ НЕПРАВИЛЬНО
pip install -r requirements.txt

# ✅ ПРАВИЛЬНО
python -m pip install -r requirements.txt
```

### Проблема: Ошибка 498 (антибот-защита)

**Симптомы:**
```
❌ Ошибка запроса страницы X: статус 498
```

**Решения:**

1. **Обновите cookies в .env:**
   - Откройте https://www.wildberries.ru в браузере
   - Дождитесь полной загрузки страницы
   - Получите cookies по инструкции из [Шага 5](#шаг-5-получение-cookies-для-wb-парсера-вручную)
   - Обновите значения в `.env`
   - Перезапустите парсер

2. **Проверьте cookies в .env:**
   - Убедитесь, что все 5 cookies указаны
   - Проверьте, что значения не пустые
   - Убедитесь, что нет лишних пробелов или кавычек
   - Проверьте формат имен переменных (дефисы заменены на подчеркивания)

3. **Проверьте формат cookies:**
   - Значения должны быть без кавычек
   - Если cookie начинается с `_`, используйте двойное подчеркивание (`WB_COOKIE__CP`)

### Проблема: Ошибка 429 (Rate Limiting)

**Симптомы:**
```
⚠️ Rate limit (429) при запросе страницы X
```

**Решение:**
Парсер автоматически обрабатывает 429 ошибки:
- Экспоненциальная задержка (2.0 сек, 4.0 сек)
- Автоматический повтор запроса
- Уменьшенный размер батчей (2 страницы вместо 3)

Если ошибки продолжаются, увеличьте задержки в коде.

### Проблема: Товары BEAUTYLAB не получают "Цена до СПП"

**Симптомы:**
```
⚠️ BEAUTYLAB: 23 товаров не найдено в ответе discounts API
```

**Решение:**
Добавьте отдельный токен для BEAUTYLAB в `.env`:

```env
WB_DISCOUNTS_API_TOKEN_BEAUTYLAB=ваш_токен_beautylab
```

Discounts API возвращает только товары того кабинета, к которому принадлежит токен. Поэтому нужны отдельные токены для каждого кабинета.

### Проблема: Python не найден

**Симптомы:**
```
'python' не является внутренней или внешней командой
```

**Решение:**

1. Установите Python с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно** отметьте "Add Python to PATH"
3. Перезапустите терминал
4. Проверьте: `python --version`

### Проблема: Модули не найдены

**Симптомы:**
```
ModuleNotFoundError: No module named 'curl_cffi'
```

**Решение:**

1. Убедитесь, что вы в правильной директории проекта
2. Установите зависимости:
   ```bash
   python -m pip install -r requirements.txt
   ```

3. Если проблема сохраняется, попробуйте:
   ```bash
   python -m pip install --upgrade pip
   python -m pip install -r requirements.txt --force-reinstall
   ```

## 📈 Производительность

- **Скорость парсинга:** ~5-15 секунд на продавца (зависит от количества товаров)
- **Параллельные запросы:** до 10 одновременных запросов к discounts API
- **Обработка батчей:** до 1000 товаров за один запрос к discounts API
- **Rate limiting:** автоматическая обработка с экспоненциальной задержкой

**Примерное время выполнения:**
- COSMO (442 товара): ~7-8 секунд
- BEAUTYLAB (23 товара): ~1-2 секунды
- Получение цен до СПП (465 товаров): ~2-3 секунды
- **Общее время:** ~12-15 секунд для двух продавцов

## 🔒 Безопасность

- ⚠️ Файл `.env` содержит секретные данные (cookies, токены) и **не коммитится** в git
- ✅ Используйте `.env_sample` как шаблон
- ✅ Никогда не публикуйте `.env` файл
- ✅ Регулярно обновляйте cookies и токены

## 📝 Логирование

Парсер создает подробные логи в папке `logs/`:

- **Уровни логирования:**
  - `INFO` - общая информация о процессе
  - `SUCCESS` - успешные операции
  - `WARNING` - предупреждения (rate limiting, отсутствие данных)
  - `ERROR` - ошибки запросов
  - `DEBUG` - детальная диагностика (включается через `debug=True`)

- **Что логируется:**
  - Время выполнения каждого этапа
  - Количество обработанных товаров
  - Статистика по кабинетам
  - Ошибки и предупреждения
  - Детали запросов к API

## 🛠️ Технические детали

### Используемые технологии

- **curl_cffi** - для обхода антибот-защиты (эмуляция TLS fingerprint Chrome)
- **aiohttp** - резервный HTTP клиент (не используется, но оставлен для совместимости)
- **pandas** - для обработки и экспорта данных
- **openpyxl** - для создания Excel файлов
- **loguru** - для структурированного логирования
- **python-dotenv** - для работы с переменными окружения

### Архитектура

- **Асинхронная работа** - использование `asyncio` для параллельных запросов
- **Модульная структура** - разделение на API клиентов, парсеров и утилит
- **Обработка ошибок** - автоматические повторы при временных ошибках
- **Независимость от браузера** - для WB парсера cookies настраиваются вручную через `.env`
- **Управление cookies** - автоматическая синхронизация cookies между кэшем и сессией curl_cffi
- **Управление cookies** - автоматическая синхронизация cookies между кэшем и сессией curl_cffi

## 📚 Дополнительная документация

- [Исследование API решений](docs/API_SOLUTIONS_RESEARCH.md) - подробное описание методов обхода антибота
- [Парсинг по брендам](docs/README_BRANDS_PARSING.md) - документация по парсингу брендов
- [Независимость от cookies](docs/COOKIES_INDEPENDENCE_SUMMARY.md) - **НОВОЕ:** изменения в архитектуре и инструкция для нового ПК
- [Настройка cookies](docs/COOKIES_SETUP.md) - детальная инструкция по настройке cookies (устарело для WB парсера)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи в папке `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `.env`
4. Убедитесь, что cookies актуальны (откройте WB в браузере)

## 📄 Лицензия

Проект для внутреннего использования компании.

## 🎯 Ozon Парсер

### Быстрый старт для Ozon

1. **Установите Playwright браузеры** (если еще не установили):
   ```bash
   scripts\install_playwright.bat
   ```
   Или вручную:
   ```bash
   python -m pip install playwright playwright-stealth
   python -m playwright install chromium
   ```

2. **Настройка `.env`:**
   ```env
   OZON_CLIENT_ID=176640
   OZON_API_KEY=your_api_key_here
   OZON_SELLER_ID_COSMO=176640
   OZON_SELLER_NAME_COSMO=cosmo-beauty
   ```

3. **Запуск:**
   ```bash
   python parse_ozon_sellers.py
   # или
   scripts\run_parse_ozon_sellers.bat
   ```

4. **Результат:**
   - Excel файл в `output/ozon_seller_prices_*.xlsx`
   - Логи в `logs/parser_*.log`

**Особенности парсинга Ozon:**
- ✅ Использует **Playwright + playwright-stealth** для обхода антиботов (быстро и надежно)
- ✅ Автоматически получает cookies через Playwright (не требует ручной настройки)
- ✅ Парсит данные из публичного entrypoint API (цены покупателя) + Seller API (цены продавца)
- ✅ Универсально работает на любом ПК (не требует настройки браузера)
- ✅ Улучшенная имитация пользователя (движения мыши, случайные задержки)
- ✅ Интеллектуальная обработка блокировок с подробной диагностикой
- ✅ Опциональная поддержка proxy для обхода IP-блокировок

**Опциональные настройки (в `.env`):**
```env
# Отладка: показать браузер (по умолчанию headless)
OZON_PLAYWRIGHT_HEADLESS=False

# Прокси для обхода блокировок
OZON_PROXY=http://proxy.example.com:8080
```

**Документация:**
- [README_OZON_PARSING.md](docs/README_OZON_PARSING.md) — основная документация
- [OZON_ANTIBOT_FIXES.md](docs/OZON_ANTIBOT_FIXES.md) — детали улучшений обхода антибота
- [IMPLEMENTED_CHANGES_SUMMARY.md](docs/IMPLEMENTED_CHANGES_SUMMARY.md) — краткое резюме изменений

## 🎯 Планы развития

- [x] Поддержка парсинга Ozon
- [ ] Research: "Цена после СПП (без OZON карты)" для Ozon
- [ ] Автоматическое обновление cookies при ошибке 498 (WB)
- [ ] Веб-интерфейс для управления парсингом
- [ ] Интеграция с базой данных
- [ ] API для получения данных парсинга

---

**Версия:** 1.0.0  
**Последнее обновление:** Январь 2026

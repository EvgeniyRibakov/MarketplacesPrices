# TODO Roadmap - План действий по улучшению парсера Ozon

**Дата создания:** 2026-01-21  
**Основано на:** IDEAS_REVIEW.md

---

## 🚨 Приоритет 1: Критически важно (делать первым)

### ✅ 1. Внедрить Cookies-as-a-Service
**Время:** 2-3 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать `tools/get_cookies.py` — скрипт для получения cookies через Playwright (запуск локально)
- [ ] Реализовать сохранение cookies в `cookies/ozon_cookies.json` (JSON формат)
- [ ] Добавить загрузку cookies из файла в `OzonCatalogAPI.__init__()`
- [ ] Обновить документацию: "Шаг 1: получи cookies локально, Шаг 2: запусти парсер на сервере"
- [ ] Добавить проверку существования файла cookies перед запуском парсера

**Файлы для изменения:**
- `tools/get_cookies.py` (новый)
- `src/api/ozon_catalog_api.py` (модификация)
- `README.md` (обновление)

**Ожидаемый результат:**
- Playwright нужен только локально для получения cookies
- На сервере парсер работает без браузерных зависимостей
- Cookies переиспользуются между запусками

---

### ✅ 2. Разделить requirements.txt
**Время:** 30 минут  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать `requirements-core.txt` с базовыми зависимостями:
  - curl_cffi
  - aiohttp
  - pandas
  - openpyxl
  - pydantic
- [ ] Создать `requirements-playwright.txt`:
  - playwright
  - playwright-stealth
- [ ] Создать `requirements-dev.txt`:
  - black
  - pytest
  - mypy
- [ ] Обновить основной `requirements.txt` (оставить для совместимости или удалить)
- [ ] Обновить `.env_sample` с комментариями о режимах установки

**Файлы для изменения:**
- `requirements-core.txt` (новый)
- `requirements-playwright.txt` (новый)
- `requirements-dev.txt` (новый)
- `.env_sample` (обновление)

**Ожидаемый результат:**
- Минимальные зависимости для хостинга (~50 MB вместо ~500 MB)
- Быстрая установка для LIGHT режима
- Чёткое разделение dev/production зависимостей

---

### ✅ 3. Создать режимы LIGHT/FULL
**Время:** 3-4 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Добавить `OZON_MODE=light|full` в `.env_sample`
- [ ] Модифицировать `OzonCatalogAPI.__init__()` для чтения режима
- [ ] В режиме `light`: использовать только curl_cffi, без Playwright fallback
- [ ] В режиме `full`: использовать curl_cffi + Playwright fallback (текущее поведение)
- [ ] Добавить логирование предупреждения для LIGHT режима: "Playwright недоступен, риск блокировок"
- [ ] Обновить `parse_ozon_sellers.py` для передачи режима в парсер

**Файлы для изменения:**
- `src/api/ozon_catalog_api.py` (модификация)
- `src/parsers/ozon_parser.py` (модификация)
- `parse_ozon_sellers.py` (модификация)
- `.env_sample` (обновление)

**Ожидаемый результат:**
- Один код работает для локального ПК (FULL) и хостинга (LIGHT)
- Простое переключение через переменную окружения
- Чёткое разделение сценариев использования

---

### ✅ 4. Фикс Seller API: режим MY_ACCOUNT vs FOREIGN_SELLER
**Время:** 2 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Добавить `OZON_ACCOUNT_TYPE=my|foreign` в `.env_sample`
- [ ] Модифицировать `OzonParser.parse_seller_catalog()` для чтения типа аккаунта
- [ ] Если `account_type == "foreign"`: пропускать шаг 2 (Seller API) полностью
- [ ] Добавить логирование: "Seller API пропущен (чужой продавец)"
- [ ] Обновить статистику в конце парсинга (убрать упоминание Seller API для foreign)

**Файлы для изменения:**
- `src/parsers/ozon_parser.py` (модификация)
- `parse_ozon_sellers.py` (модификация)
- `.env_sample` (обновление)

**Ожидаемый результат:**
- Экономия 0.9 сек на каждом запуске для чужих продавцов
- Чёткое понимание, почему Seller API не используется
- Упрощение логики парсера

---

### ✅ 5. Создать Dockerfile для HTTP-only
**Время:** 1-2 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать `Dockerfile`:
  ```dockerfile
  FROM python:3.12-slim
  WORKDIR /app
  COPY requirements-core.txt .
  RUN pip install --no-cache-dir -r requirements-core.txt
  COPY . .
  CMD ["python", "parse_ozon_sellers.py"]
  ```
- [ ] Добавить `.dockerignore` для исключения ненужных файлов
- [ ] Протестировать сборку образа: `docker build -t ozon-parser .`
- [ ] Протестировать запуск: `docker run --env-file .env ozon-parser`
- [ ] Измерить размер образа (ожидается ~150 MB)

**Файлы для изменения:**
- `Dockerfile` (новый)
- `.dockerignore` (новый)

**Ожидаемый результат:**
- Docker образ ~150 MB вместо ~1.5 GB
- Быстрая сборка (< 1 минуты)
- Готовность к деплою на Railway/Fly.io

---

**🎯 Ожидаемый результат после Приоритета 1:**
- ✅ Хостинг на Railway/Fly работает за 5 минут (git push → deploy)
- ✅ Docker образ: ~150 MB вместо ~1.5 GB
- ✅ Парсер работает на любом ПК без Playwright (если есть cookies)
- ✅ Seller API не тратит время на чужих продавцов

---

## ⚡ Приоритет 2: Важно (делать вторым)

### ✅ 6. Внедрить AdaptiveDelayer / PID-контроллер
**Время:** 3-4 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать класс `AdaptiveDelayer` в `src/utils/adaptive_delayer.py`
- [ ] Реализовать логику:
  - Если получен 403 → увеличить delay на 50%
  - Если 5 успешных запросов подряд → уменьшить delay на 20%
  - Минимальный delay: 0.5 сек, максимальный: 5.0 сек
- [ ] Интегрировать в `OzonCatalogAPI._fetch_page()`
- [ ] Добавить логирование изменений delay
- [ ] Добавить опцию в .env: `OZON_ADAPTIVE_DELAY=true` (по умолчанию true)

**Файлы для изменения:**
- `src/utils/adaptive_delayer.py` (новый)
- `src/api/ozon_catalog_api.py` (модификация)
- `.env_sample` (обновление)

**Ожидаемый результат:**
- Умная адаптация к антиботу без ручной настройки
- Автоматическое снижение скорости при блокировках
- Увеличение скорости при успешных запросах

---

### ✅ 7. Централизованная конфигурация (Config dataclass)
**Время:** 2-3 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать `src/config.py`:
  ```python
  from dataclasses import dataclass
  from enum import Enum
  
  class Mode(Enum):
      LIGHT = "light"
      FULL = "full"
  
  @dataclass
  class OzonConfig:
      mode: Mode
      request_delay: float
      max_concurrent: int
      cookies_path: str
      account_type: str  # my/foreign
      # ... другие параметры
  ```
- [ ] Реализовать функцию `load_config()` для чтения из .env
- [ ] Заменить прямое чтение .env во всех модулях на использование OzonConfig
- [ ] Добавить валидацию параметров в dataclass
- [ ] Обновить все классы: OzonParser, OzonCatalogAPI, OzonSellerAPI

**Файлы для изменения:**
- `src/config.py` (новый)
- `src/parsers/ozon_parser.py` (модификация)
- `src/api/ozon_catalog_api.py` (модификация)
- `src/api/ozon_seller_api.py` (модификация)
- `parse_ozon_sellers.py` (модификация)

**Ожидаемый результат:**
- Чистая архитектура конфигурации
- Легко тестировать (можно передать mock config)
- Простое расширение новых параметров

---

### ✅ 8. Авто-диагностика Seller API
**Время:** 1 час  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] В `OzonParser.parse_seller_catalog()` перед основным запросом:
  - Сделать тестовый запрос с 1 SKU (первый из списка)
  - Проверить результат: если `total == 0` → пропустить Seller API
- [ ] Добавить логирование: "Seller API диагностика: 0 товаров найдено, пропускаем"
- [ ] Обновить статистику: убрать упоминание Seller API если он пропущен

**Файлы для изменения:**
- `src/parsers/ozon_parser.py` (модификация)

**Ожидаемый результат:**
- Быстрое определение проблемы с Seller API
- Экономия времени на бесполезных запросах
- Понятные логи для диагностики

---

### ✅ 9. Скрипт авто-установки
**Время:** 1 час  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать `install.sh` (Linux/Mac):
  ```bash
  #!/bin/bash
  python3 -m venv venv
  source venv/bin/activate
  pip install -r requirements-core.txt
  echo "✅ Установка завершена (LIGHT режим)"
  echo "Для FULL режима: pip install -r requirements-playwright.txt && playwright install chromium"
  ```
- [ ] Создать `install.bat` (Windows) — аналогично
- [ ] Сделать скрипты исполняемыми: `chmod +x install.sh`
- [ ] Обновить README с инструкциями по использованию

**Файлы для изменения:**
- `install.sh` (новый)
- `install.bat` (новый)
- `README.md` (обновление)

**Ожидаемый результат:**
- Установка на новом ПК: 1 команда
- Автоматическое создание venv
- Понятные инструкции для пользователей

---

### ✅ 10. Railway/Fly.io templates
**Время:** 1-2 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать `railway.toml`:
  ```toml
  [build]
  builder = "dockerfile"
  
  [deploy]
  startCommand = "python parse_ozon_sellers.py"
  ```
- [ ] Создать `fly.toml` (аналогично)
- [ ] Создать `Dockerfile.railway` (опционально, если нужны отличия)
- [ ] Добавить секцию "Deploy to Railway" в README с кнопкой [![Deploy on Railway](https://railway.app/button.svg)](...)
- [ ] Добавить инструкции по настройке переменных окружения на Railway/Fly

**Файлы для изменения:**
- `railway.toml` (новый)
- `fly.toml` (новый)
- `Dockerfile.railway` (новый, опционально)
- `README.md` (обновление)

**Ожидаемый результат:**
- Деплой на Railway/Fly: 1 кнопка
- Автоматическая сборка Docker образа
- Готовность к production использованию

---

**🎯 Ожидаемый результат после Приоритета 2:**
- ✅ Парсер умный (адаптивные задержки)
- ✅ Конфигурация чистая (Config dataclass)
- ✅ Установка на новом ПК: 1 команда
- ✅ Хостинг: 1 кнопка

---

## 💡 Приоритет 3: Полезно (делать третьим)

### ✅ 11. Кэш страниц каталога
**Время:** 2 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Установить `diskcache` в requirements-core.txt
- [ ] Создать `src/utils/cache.py` с классом `CatalogCache`
- [ ] Реализовать сохранение страниц в `.cache/seller_{id}_page_{n}.json`
- [ ] TTL = 24 часа (настраиваемо через .env)
- [ ] Добавить `OZON_CACHE_ENABLED=true` в .env_sample
- [ ] Интегрировать в `OzonCatalogAPI._fetch_page()`:
  - Проверять кэш перед запросом
  - Сохранять в кэш после успешного запроса
  - Использовать кэш при ошибке (fallback)

**Файлы для изменения:**
- `src/utils/cache.py` (новый)
- `src/api/ozon_catalog_api.py` (модификация)
- `requirements-core.txt` (обновление)
- `.env_sample` (обновление)

**Ожидаемый результат:**
- Повторные запуски быстрее (секунды вместо минут)
- Fallback на кэш при ошибках
- Удобно для отладки

---

### ✅ 12. Makefile для команд
**Время:** 30 минут  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Создать `Makefile`:
  ```makefile
  install:
  	pip install -r requirements-core.txt
  
  install-dev:
  	pip install -r requirements-core.txt -r requirements-playwright.txt
  	playwright install chromium
  
  run:
  	python parse_ozon_sellers.py
  
  docker-build:
  	docker build -t ozon-parser .
  
  docker-run:
  	docker run --env-file .env ozon-parser
  ```
- [ ] Протестировать команды на Linux/Mac
- [ ] Добавить инструкции в README

**Файлы для изменения:**
- `Makefile` (новый)
- `README.md` (обновление)

**Ожидаемый результат:**
- Единообразные команды для всех платформ
- Упрощение работы с проектом
- Стандартный интерфейс для разработчиков

---

### ✅ 13. Переиспользование Playwright browser (для FULL режима)
**Время:** 2-3 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Модифицировать `OzonCatalogAPI` для хранения browser instance как атрибута класса
- [ ] В `_fetch_page_via_playwright()`: проверять, есть ли уже открытый browser
- [ ] Если browser нет → создать новый
- [ ] Если browser есть → переиспользовать
- [ ] Закрывать browser только в `__aexit__` контекст-менеджера
- [ ] Добавить обработку ошибок (если browser упал → создать новый)

**Файлы для изменения:**
- `src/api/ozon_catalog_api.py` (модификация)

**Ожидаемый результат:**
- Playwright fallback быстрее (~2-3 сек вместо 5-10 сек на страницу)
- Меньше потребление ресурсов
- Стабильнее работа браузера

---

### ✅ 14. Health check endpoint (опционально)
**Время:** 2 часа  
**Статус:** ⏳ Ожидает выполнения

**Задачи:**
- [ ] Добавить `fastapi` в requirements-core.txt (опционально) или requirements-dev.txt
- [ ] Создать `src/api/health.py` с FastAPI приложением
- [ ] Endpoint `/health`:
  ```python
  {
    "status": "ok",
    "last_run": "2026-01-21T18:36:50",
    "last_products": 524,
    "mode": "light"
  }
  ```
- [ ] Запускать только если `OZON_MODE=light` (server режим)
- [ ] Добавить инструкции по мониторингу через UptimeRobot/Healthchecks.io

**Файлы для изменения:**
- `src/api/health.py` (новый)
- `parse_ozon_sellers.py` (модификация)
- `requirements-core.txt` или `requirements-dev.txt` (обновление)
- `README.md` (обновление)

**Ожидаемый результат:**
- Мониторинг работы парсера на хостинге
- Быстрое обнаружение проблем
- Интеграция с внешними сервисами мониторинга

---

**🎯 Ожидаемый результат после Приоритета 3:**
- ✅ Повторные запуски быстрее (кэш)
- ✅ Команды единообразные (Makefile)
- ✅ Playwright fallback быстрее (~2-3 сек вместо 5-10 сек)
- ✅ Мониторинг работает (если нужен)

---

## 📊 Статистика задач

**Всего задач:** 14  
**Приоритет 1:** 5 задач (~9-12 часов)  
**Приоритет 2:** 5 задач (~9-12 часов)  
**Приоритет 3:** 4 задачи (~6-8 часов)  

**Общее время:** ~24-32 часа работы

---

## 🎯 ТОП-3 критических задачи (максимальный ROI)

Если делать только самое важное:

1. **Cookies-as-a-Service** → Playwright только локально, на сервере HTTP-only
2. **Режимы LIGHT/FULL** → Один код для всех сценариев
3. **Dockerfile без Playwright** → Деплой на Railway/Fly за 5 минут

**Время на ТОП-3:** ~6-9 часов  
**Результат:** Проект готов к хостингу, работает на любом ПК

---

**Дата создания:** 2026-01-21  
**Версия:** 1.0

# Исправление: Пагинация не работает (только 12 товаров)

## 🔍 Root Cause Analysis

### Проблема
Парсер получает только 12 товаров (первая страница), хотя у продавца может быть больше товаров. Пагинация не работает.

### Доказательства из логов
- **Строка 137**: Ключи в ответе: `['layout', 'widgetStates', 'browser', 'client', 'layoutTrackingInfo', 'location', 'seo', 'shared', 'segments', 'pageInfo']`
- **Строка 138**: `nextPage присутствует: НЕТ`
- **Строка 139**: `Найдено поле 'next' в ответе` (но не используется!)
- **Строка 140**: Получено только 12 товаров со страницы 1
- **Строка 141**: Всего товаров 12, страниц успешно 1

### Root Cause (корневая причина)
**Строка 900 в `src/api/ozon_catalog_api.py`**:
```python
next_page_url = first_page_data.get("nextPage")
```

Код ищет поле `nextPage` в корневом уровне ответа, но:
1. ❌ Поле `nextPage` отсутствует в корневом уровне
2. ✅ Поле `next` найдено в ответе (строка 139), но не используется
3. ✅ Информация о пагинации находится в `widgetStates` → `tileGridDesktop`, а не в корневом уровне

### Логическая ошибка
**Строка 923**:
```python
while next_page_url and page < max_pages:
```

Цикл не выполняется, потому что `next_page_url` всегда `None` (поле `nextPage` не найдено).

## ✅ Решение

### Что исправлено

#### 1. Добавлен поиск пагинации в widgetStates (строки 910-948)
Код теперь ищет информацию о пагинации в правильном месте:
- Проверяет `widgetStates` → `tileGridDesktop` (где находятся данные о товарах)
- Ищет различные варианты полей: `nextPage`, `next`, `pagination`, `paginatorToken`, `searchPageState`
- Проверяет корневой уровень, если не найдено в widgetStates

#### 2. Обновлена логика пагинации в цикле (строки 975-1035)
- Извлекает параметры пагинации из найденных полей
- Использует `paginator_token` и `search_page_state` для загрузки следующих страниц
- Ищет информацию о следующей странице в каждом ответе
- Правильно обрабатывает различные форматы данных (`next` как строка или объект)

#### 3. Добавлено детальное логирование
- Показывает, где именно найдена информация о пагинации
- Логирует структуру данных для диагностики
- Помогает понять, почему пагинация не работает

### Изменения в коде

**Было:**
```python
next_page_url = first_page_data.get("nextPage")  # Ищет только в корневом уровне
while next_page_url and page < max_pages:
    # ...
    next_page_url = page_data.get("nextPage")  # Ищет только в корневом уровне
```

**Стало:**
```python
# Ищем пагинацию в widgetStates (tileGridDesktop)
widget_states = first_page_data.get("widgetStates", {})
for state_id, state_json in widget_states.items():
    if "tileGridDesktop" in state_id:
        state_data = json.loads(state_json)
        # Проверяем различные варианты полей пагинации
        if "nextPage" in state_data:
            next_page_url = state_data.get("nextPage")
        elif "next" in state_data:
            next_val = state_data.get("next")
            # Обрабатываем как строку или объект
        # ... и т.д.

# Также проверяем корневой уровень
if not next_page_url:
    if "next" in first_page_data:
        # ...
```

## 📊 Ожидаемый результат

### До исправления:
```
Страница 1 → 12 товаров → nextPage не найден → остановка
```

### После исправления:
```
Страница 1 → 12 товаров → найдена пагинация в tileGridDesktop → 
Страница 2 → 12 товаров → найдена пагинация → 
Страница 3 → ... → 
Пока есть следующая страница или достигнут max_pages
```

## 🔧 Технические детали

### Структура ответа Ozon API
```json
{
  "widgetStates": {
    "tileGridDesktop-...": {
      "items": [...],
      "nextPage": "...",  // или "next", или "pagination"
      "paginatorToken": "...",
      "searchPageState": "..."
    }
  },
  "next": "...",  // альтернативное поле в корневом уровне
  "pagination": {...}  // альтернативное поле в корневом уровне
}
```

### Алгоритм поиска пагинации
1. Проверяем `widgetStates` → `tileGridDesktop` (приоритет)
2. Проверяем корневой уровень ответа (fallback)
3. Извлекаем `paginator_token` и `search_page_state` из найденных полей
4. Используем эти параметры для запроса следующей страницы

## 📝 Файлы изменены
- `src/api/ozon_catalog_api.py`:
  - Добавлен поиск пагинации в widgetStates (строки 910-948)
  - Обновлена логика пагинации в цикле (строки 975-1035)
  - Добавлено детальное логирование

## 🚀 Что делать дальше

1. **Запустить парсинг** - проверить, что пагинация работает
2. **Проверить логи** - убедиться, что информация о пагинации найдена
3. **Мониторить результаты** - проверить, что загружаются все страницы

## ⚠️ Важные замечания

1. **Структура ответа может отличаться** - код проверяет различные варианты полей
2. **Пагинация может отсутствовать** - если у продавца действительно только одна страница товаров
3. **Лимит страниц** - по умолчанию `max_pages=100`, можно изменить при вызове

## 🔍 Диагностика

Если пагинация всё ещё не работает, проверьте логи:
- Найдена ли информация о пагинации в `tileGridDesktop`?
- Какие поля присутствуют в `state_data`?
- Есть ли `paginatorToken` или `searchPageState`?

Логи покажут детальную информацию о структуре ответа и помогут понять, почему пагинация не работает.

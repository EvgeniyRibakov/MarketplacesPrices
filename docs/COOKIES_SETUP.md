# Настройка Cookies для парсера Wildberries

## Зачем нужны cookies?

Wildberries использует систему защиты от ботов (антибот), которая требует наличия специальных cookies из браузера. Без них запросы будут блокироваться с ошибкой 498.

⚠️ **ВАЖНО**: Токен `x-wbaas-token` обновляется каждые 5-15 минут, поэтому ручное обновление cookies неэффективно.

## 🚀 Автоматическое получение cookies (РЕКОМЕНДУЕТСЯ)

Парсер **автоматически получает cookies из браузера Chrome** при запуске и обновляет их при ошибке 498. Это избавляет от необходимости вручную копировать cookies.

### Требования:
- Установленные зависимости: `python -m pip install -r requirements.txt`
- Chrome браузер должен быть установлен на системе
- При первом запуске может потребоваться открыть Wildberries в браузере для получения cookies

### Как это работает:
1. Парсер автоматически пытается прочитать cookies из базы данных Chrome
2. Если база заблокирована (Chrome запущен), используется headless Chrome
3. При ошибке 498 cookies автоматически обновляются из браузера
4. Не требует ручного копирования cookies

### Отключение автоматического получения:
Если нужно использовать cookies из `.env` файла, установите `auto_get_cookies=False` при создании API клиента.

## Ручное получение cookies (если автоматическое не работает)

Если автоматическое получение не работает, можно вручную скопировать cookies:

### Chrome/Edge:

1. Откройте сайт https://www.wildberries.ru
2. Нажмите `F12` для открытия DevTools
3. Перейдите на вкладку **Application** (Приложение)
4. В левом меню найдите **Cookies** → **https://www.wildberries.ru**
5. Найдите следующие cookies и скопируйте их значения:
   - `wbx-validation-key`
   - `_cp`
   - `routeb`
   - `x_wbaas_token` ⚠️ **ВАЖНО!** Это токен антибота (обновляется каждые 5-15 минут)
   - `_wbauid`

### Firefox:

1. Откройте сайт https://www.wildberries.ru
2. Нажмите `F12` для открытия DevTools
3. Перейдите на вкладку **Хранилище** (Storage)
4. Найдите **Cookies** → **https://www.wildberries.ru**
5. Скопируйте значения тех же cookies

## Как добавить cookies в .env файл

Откройте файл `.env` в корне проекта и добавьте следующие строки:

```env
# Cookies из браузера для обхода антибота
WB_COOKIE_WBX_VALIDATION_KEY=ваше_значение_здесь
WB_COOKIE__CP=ваше_значение_здесь
WB_COOKIE_ROUTEB=ваше_значение_здесь
WB_COOKIE_X_WBAAS_TOKEN=ваше_значение_здесь
WB_COOKIE__WBAUID=ваше_значение_здесь
```

**Важно:**
- Замените `ваше_значение_здесь` на реальные значения из браузера
- Не используйте кавычки вокруг значений
- Cookies могут истекать, поэтому их нужно периодически обновлять

## Пример заполненного .env

```env
WB_DEST=-1257786
WB_SPP=30

WB_COOKIE_WBX_VALIDATION_KEY=abc123def456
WB_COOKIE__CP=xyz789
WB_COOKIE_ROUTEB=route_value
WB_COOKIE_X_WBAAS_TOKEN=token_abc123xyz789
WB_COOKIE__WBAUID=wbauid_value
```

## Проверка работы

После добавления cookies запустите парсер:

```bash
python parse_brands.py
```

Если cookies корректны, вы увидите в логах:
```
Используются cookies из .env файла
Загружено 5 cookies из конфигурации: wbx-validation-key, _cp, routeb, x_wbaas_token, _wbauid
```

## Автоматическое обновление cookies

При ошибке 498 парсер **автоматически обновляет cookies из браузера**. Не требуется ручное вмешательство.

### Логи при автоматическом обновлении:
```
Ошибка 498 при запросе...
Обновление cookies из браузера...
✓ Cookies обновлены из браузера
Повторная попытка запроса...
```

## Ручное обновление cookies (если автоматическое не работает)

Если автоматическое обновление не сработало:

1. Откройте сайт Wildberries в браузере
2. Получите свежие cookies по инструкции выше
3. Обновите значения в `.env` файле
4. Перезапустите парсер

## Устранение проблем

### Проблема: "База данных cookies заблокирована Chrome"
**Решение**: Закройте Chrome или используйте headless режим (автоматически включается)

### Проблема: "Не удалось получить cookies из браузера"
**Решение**: 
- Убедитесь, что Chrome установлен
- Откройте https://www.wildberries.ru в браузере хотя бы раз
- Проверьте, что установлены все зависимости: `python -m pip install -r requirements.txt`

### Проблема: "undetected-chromedriver не установлен"
**Решение**: `python -m pip install undetected-chromedriver selenium`

# Парсер цен для Ozon

Документация по парсеру цен товаров продавцов Ozon.

## 🎯 Описание

Парсер собирает данные о ценах товаров с маркетплейса Ozon для аналитики и мониторинга цен. Использует два источника данных:

1. **Ozon Seller API** - официальное API для продавцов
   - Цена продавца (без маркетинговых акций)
   - Зачёркнутая цена (установленная продавцом)
   - Минимальная цена

2. **Entrypoint API** - публичный каталог продавца
   - Цена покупателя (текущая цена со всеми скидками)
   - Зачёркнутая цена (отображаемая на витрине)
   - Процент скидки

## 📊 Какие данные собираются

### Основные данные товара:
- **SKU** - уникальный идентификатор товара на Ozon
- **Артикул продавца** (offer_id) - артикул продавца
- **Название товара**
- **ID кабинета** - идентификатор продавца
- **Кабинет** - название кабинета продавца

### Цены из публичного каталога (что видит покупатель):
- **Цена покупателя** - текущая цена со всеми скидками
- **Зачёркнутая цена (каталог)** - старая цена, отображаемая на витрине
- **Скидка %** - процент скидки

### Цены из Seller API (данные продавца):
- **Цена продавца** - цена без маркетинговых акций
- **Зачёркнутая цена (API)** - зачёркнутая цена из API продавца
- **Минимальная цена** - минимальная цена за период

## 🚀 Быстрый старт

### 1. Настройка конфигурации

Создайте файл `.env` из шаблона:

```bash
copy .env_sample .env
```

Откройте `.env` и укажите ваши данные:

```env
# Ozon Seller API credentials
OZON_CLIENT_ID=176640
OZON_API_KEY=your_ozon_api_key_here

# Продавец для парсинга
OZON_SELLER_ID_COSMO=176640
OZON_SELLER_NAME_COSMO=cosmo-beauty
```

**Где получить Client ID и API Key:**

1. Зайдите в личный кабинет продавца Ozon
2. Перейдите в **Настройки** → **Seller API**
3. Нажмите **"Создать токен"**
4. Скопируйте **Client-Id** и **Api-Key** в `.env`

**Как узнать Seller ID и Seller Name:**

1. Откройте страницу вашего магазина на Ozon:
   ```
   https://www.ozon.ru/seller/cosmo-beauty-176640/
   ```
2. Из URL извлеките:
   - `seller_name`: `cosmo-beauty` (название перед цифрами)
   - `seller_id`: `176640` (цифры в конце)

### 2. Запуск парсера

**Вариант 1: Через Python**

```bash
python parse_ozon_sellers.py
```

**Вариант 2: Через батч-файл (Windows)**

```bash
scripts\run_parse_ozon_sellers.bat
```

### 3. Результаты

После выполнения парсера:

- **Excel файл** создается в папке `output/`:
  - Формат: `ozon_seller_prices_YYYY-MM-DD_HH-MM-SS.xlsx`
  - Автоматическая настройка ширины столбцов
  - Сортировка по названию товара

- **Логи** сохраняются в папке `logs/`:
  - Формат: `parser_YYYY-MM-DD.log`
  - Детальная информация о каждом запросе
  - Статистика по времени выполнения

## 📁 Структура файлов

```
MParsing/
├── src/
│   ├── api/
│   │   ├── ozon_seller_api.py      # Клиент для Ozon Seller API
│   │   └── ozon_catalog_api.py     # Клиент для публичного каталога
│   └── parsers/
│       └── ozon_parser.py          # Высокоуровневый парсер
│
├── scripts/
│   └── run_parse_ozon_sellers.bat  # Скрипт запуска (Windows)
│
├── docs/
│   └── README_OZON_PARSING.md      # Эта документация
│
├── output/                          # Результаты парсинга (создается автоматически)
│   └── ozon_seller_prices_*.xlsx
│
├── logs/                            # Логи (создается автоматически)
│   └── parser_*.log
│
├── parse_ozon_sellers.py           # Главный скрипт парсинга
├── .env                             # Конфигурация (не коммитится)
└── .env_sample                      # Шаблон конфигурации
```

## ⚙️ Архитектура

### 1. OzonSellerAPI (`src/api/ozon_seller_api.py`)

Клиент для работы с официальным Ozon Seller API.

**Основные методы:**
- `fetch_product_prices()` - получение цен товаров по SKU или offer_id
- `parse_price_item()` - парсинг товара из ответа API

**Особенности:**
- Использует `curl_cffi` для эмуляции браузера
- Поддержка пагинации (cursor-based)
- Автоматическая обработка rate limiting (429)
- Безопасная задержка между запросами: 0.5 сек

### 2. OzonCatalogAPI (`src/api/ozon_catalog_api.py`)

Клиент для работы с публичным каталогом продавца (entrypoint API).

**Основные методы:**
- `fetch_seller_catalog()` - получение каталога продавца (все страницы)
- `parse_products_from_page()` - парсинг товаров со страницы
- `parse_product()` - парсинг одного товара

**Особенности:**
- Парсинг JSON из `widgetStates`
- Поддержка пагинации через `nextPage`
- Инициализация сессии с главной страницы
- Безопасная задержка между запросами: 1.0 сек

### 3. OzonParser (`src/parsers/ozon_parser.py`)

Высокоуровневый парсер, объединяющий данные из двух источников.

**Основные методы:**
- `parse_seller_catalog()` - парсинг каталога продавца

**Логика работы:**
1. Получение товаров из публичного каталога (entrypoint API)
2. Получение цен продавца через Seller API
3. Объединение данных по SKU

## 🔧 Настройки

### Задержки между запросами

В `.env` можно настроить задержки:

```env
# Задержка между запросами (секунды)
REQUEST_DELAY=0.5          # Для Seller API
```

Для entrypoint API задержка жестко установлена на 1.0 сек (безопасное значение).

### Количество параллельных запросов

```env
# Количество параллельных запросов
MAX_CONCURRENT_REQUESTS=5   # Для Seller API (по умолчанию)
```

Для entrypoint API: 3 параллельных запроса (безопасное значение).

### Максимальное количество страниц

В коде `OzonCatalogAPI.fetch_seller_catalog()`:

```python
max_pages: int = 100  # Защита от бесконечного цикла
```

## 📈 Производительность

- **Seller API**: ~1-2 секунды на 100 товаров
- **Catalog API**: ~3-5 секунд на страницу (зависит от количества товаров)
- **Объединение данных**: мгновенно

**Примерное время выполнения:**
- Каталог продавца (50 товаров, 3 страницы): ~10-15 секунд
- Получение цен через Seller API (50 товаров): ~1-2 секунды
- **Общее время**: ~12-20 секунд

## ⚠️ Ограничения и известные проблемы

### 1. Rate Limiting

**Seller API:**
- Используется безопасная задержка 0.5 сек между запросами
- Автоматическая обработка 429 ошибок с повтором

**Entrypoint API:**
- Используется безопасная задержка 1.0 сек между запросами
- Ограничение: максимум 100 страниц (защита от бесконечного цикла)

### 2. Цена после СПП (без OZON карты)

**Статус:** требует research

Текущие данные:
- ✅ Цена покупателя (текущая цена со всеми скидками)
- ✅ Цена продавца (без маркетинговых акций)
- ❌ Цена после СПП (без OZON карты) - **не найдена**

Возможные варианты:
1. Расчетное значение между ценой продавца и ценой покупателя
2. Отдельное поле в API (требует research)
3. Парсинг дополнительных данных со страницы товара

### 3. Товары не найдены в Seller API

Некоторые товары могут присутствовать в публичном каталоге, но отсутствовать в Seller API.

**Причины:**
- Товар принадлежит другому продавцу (перекуп)
- Товар временно недоступен через API
- Товар заблокирован или скрыт

**Решение:**
- Парсер логирует такие товары
- Данные из каталога все равно сохраняются в Excel
- Поля из Seller API будут пустыми

## 🐛 Решение проблем

### Ошибка: "401 Unauthorized"

**Причина:** Неверный Client-Id или Api-Key

**Решение:**
1. Проверьте правильность Client-Id и Api-Key в `.env`
2. Убедитесь, что токен не истек
3. Создайте новый токен в личном кабинете Ozon

### Ошибка: "429 Too Many Requests"

**Причина:** Превышен лимит запросов

**Решение:**
1. Увеличьте задержку в коде:
   ```python
   request_delay=1.0  # Вместо 0.5
   ```
2. Уменьшите количество параллельных запросов
3. Подождите несколько минут и повторите

### Ошибка: "Не удалось получить первую страницу"

**Причина:** Неверный seller_id или seller_name

**Решение:**
1. Проверьте правильность `OZON_SELLER_ID_COSMO` и `OZON_SELLER_NAME_COSMO` в `.env`
2. Откройте страницу продавца в браузере и проверьте URL
3. Убедитесь, что продавец существует и доступен

### Пустой Excel файл

**Причина:** Не получено товаров из каталога

**Решение:**
1. Проверьте логи в папке `logs/`
2. Убедитесь, что у продавца есть товары
3. Проверьте, что seller_name и seller_id правильные

## 📚 Примеры использования

### Пример 1: Базовый запуск

```bash
python parse_ozon_sellers.py
```

### Пример 2: Запуск с просмотром логов

```bash
python parse_ozon_sellers.py
# Откройте logs/parser_YYYY-MM-DD.log для просмотра подробных логов
```

### Пример 3: Изменение продавца

В `.env`:

```env
OZON_SELLER_ID_COSMO=123456
OZON_SELLER_NAME_COSMO=your-seller-name
```

## 🔐 Безопасность

- ⚠️ Файл `.env` содержит секретные данные (API ключи) и **не коммитится** в git
- ✅ Используйте `.env_sample` как шаблон
- ✅ Никогда не публикуйте `.env` файл
- ✅ Регулярно обновляйте API ключи

## 📝 TODO / Планы развития

- [ ] Research: найти способ получения "Цены после СПП (без OZON карты)"
- [ ] Добавить поддержку нескольких кабинетов
- [ ] Добавить парсинг отзывов и рейтингов
- [ ] Добавить исторические данные о ценах
- [ ] Интеграция с базой данных
- [ ] Веб-интерфейс для управления парсингом

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи в папке `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `.env`
4. Убедитесь, что API ключи актуальны

---

**Версия:** 1.0.0  
**Последнее обновление:** Январь 2026

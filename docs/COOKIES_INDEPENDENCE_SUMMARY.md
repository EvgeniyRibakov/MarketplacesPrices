# 📋 Саммари: Изменения в независимости от cookies

## 🎯 Что изменилось

### ✅ Удалена зависимость от браузера Chrome

**Было:**
- Парсер автоматически получал cookies из базы данных Chrome
- Требовалась установка `undetected-chromedriver` и `selenium`
- Нужен был доступ к файлам Chrome User Data
- Могли быть проблемы с правами доступа к файлам браузера

**Стало:**
- Парсер **не требует** установленного Chrome или доступа к его файлам
- Cookies предоставляются **вручную** через `.env` файл
- Удалены методы `_load_cookies_from_browser()` и `_refresh_cookies_from_browser()`
- Удален параметр `auto_get_cookies` из `WBCatalogAPI`

### ✅ Улучшена работа с cookies в curl_cffi

**Изменения:**
- Cookies теперь **явно добавляются** в `session.cookies` curl_cffi
- Исправлена синхронизация cookies между кэшем и сессией
- Добавлена безопасная обработка разных форматов cookies (строки, объекты, кортежи)
- Улучшено логирование количества cookies в сессии

**Технические детали:**
- Метод `_load_custom_cookies()` теперь добавляет cookies в `session.cookies.set()`
- Используется домен `.wildberries.ru` для работы на всех поддоменах
- Добавлена обработка исключений при работе с cookies

### ⚠️ Cookies всё ещё необходимы

**Важно:** Полностью без cookies работать **нельзя**. Wildberries возвращает ошибку 498 (антибот блокировка) без cookies.

**Что нужно:**
- Cookies **обязательно** должны быть указаны в `.env` файле
- Минимум 5 cookies: `wbx-validation-key`, `_cp`, `routeb`, `x_wbaas_token`, `_wbauid`
- Cookies можно получить вручную из браузера (см. инструкцию ниже)

## 📝 Что нужно сделать на новом ПК

### Шаг 1: Установка зависимостей

**Удалены зависимости:**
- ❌ `undetected-chromedriver` (больше не нужен)
- ❌ `selenium` (больше не нужен)

**Остались зависимости:**
- ✅ `curl-cffi` - для обхода антибота
- ✅ `pandas` - для работы с данными
- ✅ `openpyxl` - для экспорта в Excel
- ✅ `loguru` - для логирования
- ✅ `python-dotenv` - для работы с .env

**Команда установки:**
```bash
python -m pip install -r requirements.txt
```

### Шаг 2: Настройка .env файла

1. **Скопируйте `.env_sample` в `.env`:**
   ```bash
   copy .env_sample .env
   ```

2. **Получите cookies вручную:**

   **Способ 1: Из Chrome (рекомендуется)**
   - Откройте https://www.wildberries.ru в Chrome
   - Нажмите `F12` → вкладка **Application** → **Cookies** → `https://www.wildberries.ru`
   - Найдите и скопируйте значения следующих cookies:
     - `wbx-validation-key`
     - `_cp`
     - `routeb`
     - `x_wbaas_token`
     - `_wbauid`

   **Способ 2: Из DevTools (альтернатива)**
   - Откройте https://www.wildberries.ru
   - Нажмите `F12` → вкладка **Network**
   - Обновите страницу (`F5`)
   - Найдите любой запрос к `wildberries.ru`
   - Откройте вкладку **Headers** → секция **Request Headers**
   - Найдите заголовок `Cookie:` и скопируйте значения

3. **Добавьте cookies в `.env`:**
   ```env
   # Cookies для обхода антибота WB (обязательно!)
   WB_COOKIE_WBX_VALIDATION_KEY=ваше_значение_здесь
   WB_COOKIE__CP=ваше_значение_здесь
   WB_COOKIE_ROUTEB=ваше_значение_здесь
   WB_COOKIE_X_WBAAS_TOKEN=ваше_значение_здесь
   WB_COOKIE__WBAUID=ваше_значение_здесь
   ```

   **Важно:** 
   - Значения cookies должны быть **без кавычек**
   - Если cookie начинается с `_`, используйте двойное подчеркивание в имени переменной (`WB_COOKIE__CP`)
   - Дефисы заменяются на подчеркивания (`wbx-validation-key` → `WB_COOKIE_WBX_VALIDATION_KEY`)

4. **Настройте токены для discounts API (опционально, но рекомендуется):**
   ```env
   # Токены для получения цен до СПП
   WB_DISCOUNTS_API_TOKEN_COSMO=ваш_токен_cosmo
   WB_DISCOUNTS_API_TOKEN_BEAUTYLAB=ваш_токен_beautylab
   ```

### Шаг 3: Запуск парсера

```bash
python parse_brands.py
```

## 🔄 Обновление cookies

**Когда обновлять:**
- При ошибке 498 (антибот блокировка)
- Если парсер перестал работать
- Раз в несколько дней (cookies могут истекать)

**Как обновить:**
1. Откройте https://www.wildberries.ru в браузере
2. Дождитесь полной загрузки страницы
3. Получите cookies по инструкции выше
4. Обновите значения в `.env`
5. Перезапустите парсер

## 📊 Преимущества новой архитектуры

### ✅ Упрощение установки
- Не нужен Chrome браузер
- Не нужны дополнительные библиотеки для работы с браузером
- Меньше зависимостей = быстрее установка

### ✅ Кроссплатформенность
- Работает на любом ПК (Windows, Linux, macOS)
- Не зависит от путей к файлам Chrome
- Не требует прав доступа к системным папкам браузера

### ✅ Надежность
- Меньше точек отказа (нет зависимости от Chrome)
- Пользователь контролирует cookies напрямую
- Легче диагностировать проблемы

### ⚠️ Недостатки
- Нужно вручную получать cookies (но это делается один раз)
- Cookies могут истекать (но это было и раньше)

## 🐛 Решение проблем

### Проблема: Ошибка 498 (антибот блокировка)

**Причина:** Cookies устарели или отсутствуют

**Решение:**
1. Проверьте, что все 5 cookies указаны в `.env`
2. Обновите cookies из браузера
3. Убедитесь, что значения не пустые

### Проблема: Cookies не загружаются

**Причина:** Неправильный формат в `.env`

**Решение:**
- Проверьте формат переменных (см. примеры выше)
- Убедитесь, что нет лишних пробелов или кавычек
- Проверьте, что дефисы заменены на подчеркивания

### Проблема: AttributeError при работе с cookies

**Причина:** Проблема с форматом cookies в curl_cffi (исправлено)

**Решение:** Обновите код до последней версии (исправления уже внесены)

## 📚 Дополнительная информация

- [README.md](../README.md) - основная документация проекта
- [.env_sample](../.env_sample) - пример конфигурации
- [API_SOLUTIONS_RESEARCH.md](API_SOLUTIONS_RESEARCH.md) - исследование методов обхода антибота

---

**Дата обновления:** Январь 2026  
**Версия:** 2.0.0 (без зависимости от браузера)

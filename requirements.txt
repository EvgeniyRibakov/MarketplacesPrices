# Основной файл requirements.txt для совместимости
# Рекомендуется использовать раздельные файлы:
# - requirements-core.txt (для LIGHT режима, хостинг)
# - requirements-playwright.txt (для FULL режима, локальный ПК)
# - requirements-dev.txt (для разработки)

# Основные зависимости (всегда нужны)
-r requirements-core.txt

# Playwright зависимости (только для FULL режима)
# Раскомментируйте если нужен Playwright:
# -r requirements-playwright.txt